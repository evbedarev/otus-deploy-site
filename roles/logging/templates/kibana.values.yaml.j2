ingress:
  enabled: true
  annotations: {
    kubernetes.io/ingress.class: nginx
  }
  path: /
  hosts:
    - kibana.{{ ip_addr.stdout }}.nip.io

tolerations:
  - key: node-role
    operator: Equal
    value: infra
    effect: NoSchedule
